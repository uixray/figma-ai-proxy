# 🚀 Установка прокси на Yandex Cloud - Пошаговая инструкция для новичков

## Что тебе понадобится (5 минут подготовки)

1. ✅ Компьютер с Windows/Mac/Linux
2. ✅ Доступ к серверу (у тебя есть: `ssh -l uixray 158.160.161.94`)
3. ✅ 15-20 минут времени

---

## Шаг 1: Подключиться к серверу (2 минуты)

### На Windows:

**Вариант A: Через PowerShell (встроено в Windows 10/11)**

1. Нажми `Win + X`
2. Выбери **"Windows PowerShell"** или **"Терминал"**
3. Скопируй и вставь эту команду:
   ```
   ssh -l uixray 158.160.161.94
   ```
4. Нажми `Enter`
5. Если спросит "Are you sure..." → напиши `yes` и нажми `Enter`
6. Введи пароль (символы не будут видны - это нормально)

**Вариант B: Через PuTTY (если не работает PowerShell)**

1. Скачай PuTTY: https://www.putty.org/
2. Запусти PuTTY
3. В поле "Host Name" введи: `158.160.161.94`
4. Нажми "Open"
5. В окне введи: `uixray` (username)
6. Введи пароль

### На Mac/Linux:

1. Открой **Терминал** (Cmd+Space → "Terminal")
2. Скопируй и вставь:
   ```bash
   ssh -l uixray 158.160.161.94
   ```
3. Нажми `Enter`
4. Если спросит "Are you sure..." → напиши `yes` и `Enter`
5. Введи пароль

### ✅ Проверка:
Ты должен увидеть что-то вроде:
```
uixray@vm-name:~$
```

Это значит ты на сервере! 🎉

---

## Шаг 2: Скачать установочный скрипт (1 минута)

Просто **скопируй и вставь** эту команду (весь блок целиком):

```bash
cd ~ && curl -o install.sh https://raw.githubusercontent.com/your-username/figma-yandex-proxy/main/install.sh && chmod +x install.sh
```

Нажми `Enter` и подожди 2-3 секунды.

### ⚠️ Если выдаёт ошибку "command not found: curl"

Выполни сначала эту команду:
```bash
sudo apt-get update && sudo apt-get install -y curl
```
Введи пароль если попросит, затем повтори команду выше.

### ⚠️ Если GitHub недоступен

Я создал все файлы локально на твоём компьютере. Нужно будет скопировать их на сервер. Вот как:

**На Windows (PowerShell):**
```powershell
scp "D:\Dev\Claude projects\figma-yandex-proxy\install.sh" uixray@158.160.161.94:~/
```

**На Mac/Linux:**
```bash
scp "D:/Dev/Claude\ projects/figma-yandex-proxy/install.sh" uixray@158.160.161.94:~/
```

Затем на сервере:
```bash
chmod +x install.sh
```

---

## Шаг 3: Запустить установку (5-10 минут)

Теперь самое простое - запусти скрипт:

```bash
bash install.sh
```

### Что будет происходить:

Скрипт будет задавать вопросы и показывать что делает. Вот что отвечать:

#### 1. Если спросит пароль sudo:
```
[sudo] password for uixray:
```
→ Введи свой пароль (символы не видны)

#### 2. Если Node.js старый:
```
Обновить Node.js до версии 20.x? (y/n):
```
→ Нажми `y` затем `Enter`

#### 3. Если директория существует:
```
Удалить и переустановить? (y/n):
```
→ Нажми `y` затем `Enter`

#### 4. Nginx не установлен:
```
Установить Nginx? (y/n):
```
→ Нажми `y` затем `Enter`

#### 5. Выбор настройки Nginx:
```
Выберите вариант настройки:
  1) Поддомен (например: proxy.yourdomain.com)
  2) Путь на существующем домене
  3) Пропустить настройку Nginx
Ваш выбор (1/2/3):
```

**Если у тебя есть свой домен:**
→ Нажми `1` для поддомена (проще всего)
→ Введи имя поддомена, например: `proxy.твойдомен.ru`

**Если домена нет или не знаешь что выбрать:**
→ Нажми `3` (настроим потом вручную)

### ✅ Всё работает если видишь:

```
✅ Установка завершена!

📊 Статус сервера:
┌─────┬────────────────┬─────────┬─────────┐
│ id  │ name           │ mode    │ status  │
├─────┼────────────────┼─────────┼─────────┤
│ 0   │ figma-proxy    │ fork    │ online  │
└─────┴────────────────┴─────────┴─────────┘
```

---

## Шаг 4: Проверить что всё работает (2 минуты)

### Тест 1: Health check

На сервере выполни:
```bash
curl http://localhost:3001/health
```

Должен вернуть:
```json
{"status":"ok","service":"figma-yandex-proxy",...}
```

✅ **Работает!**

### Тест 2: Проверить логи

```bash
pm2 logs figma-proxy --lines 10
```

Должен показать что-то вроде:
```
✅ Proxy running on port 3001
```

---

## Шаг 5: Настроить доступ извне (5 минут)

Сейчас прокси работает только локально. Нужно открыть доступ через Nginx.

### Вариант A: У тебя уже есть Nginx и домен

1. Открой конфиг своего сайта:
   ```bash
   sudo nano /etc/nginx/sites-available/your-site
   ```

2. Найди блок `server {` и добавь **внутри него**:
   ```nginx
   location /api/yandex-proxy {
       rewrite ^/api/yandex-proxy/(.*)$ /$1 break;
       proxy_pass http://localhost:3001;
       proxy_set_header Host $host;
       proxy_set_header X-Real-IP $remote_addr;
   }
   ```

3. Сохрани файл:
   - Нажми `Ctrl + O` (сохранить)
   - Нажми `Enter`
   - Нажми `Ctrl + X` (выйти)

4. Проверь конфиг:
   ```bash
   sudo nginx -t
   ```

5. Если всё ОК - перезагрузи Nginx:
   ```bash
   sudo systemctl reload nginx
   ```

### Вариант B: Хочу отдельный поддомен

Напиши мне - я дам отдельную инструкцию для этого случая.

### Вариант C: Пока не нужен внешний доступ

Можешь пропустить этот шаг и использовать прокси только локально для тестирования.

---

## Шаг 6: Узнать URL прокси

### Если настроил путь (Вариант A):
```
https://твойдомен.ru/api/yandex-proxy/api/yandex
```

### Если настроил поддомен:
```
https://proxy.твойдомен.ru/api/yandex
```

### Если внешний доступ не настроен:
```
http://localhost:3001/api/yandex
```
(но работает только с сервера)

---

## ❓ Частые проблемы и решения

### Проблема: "Permission denied"

**Решение:**
```bash
sudo bash install.sh
```

### Проблема: "Command not found: pm2"

**Решение:**
```bash
sudo npm install -g pm2
```

### Проблема: "Port 3001 already in use"

**Решение:** Изменить порт
```bash
cd ~/apps/figma-yandex-proxy
nano .env
```
Измени `PORT=3001` на `PORT=3002`
Сохрани (Ctrl+O, Enter, Ctrl+X)
```bash
pm2 restart figma-proxy
```

### Проблема: Прокси не отвечает

**Решение:** Смотрим логи
```bash
pm2 logs figma-proxy --lines 50
```

Если видишь ошибки - скопируй и пришли мне, помогу разобраться.

---

## 🎯 Что дальше?

После установки прокси нужно **обновить Figma плагин**, чтобы он использовал твой прокси вместо прямого обращения к Yandex API.

Напиши мне когда установишь прокси и я покажу как обновить плагин (это ещё проще чем настройка сервера! 😊)

---

## 📞 Нужна помощь?

Если что-то не работает:

1. **Сделай скриншот ошибки**
2. **Скопируй текст ошибки** (если есть)
3. **Напиши мне** - разберёмся вместе!

Также можешь прислать мне:
```bash
pm2 logs figma-proxy --lines 20
```

---

## 🎉 Ты молодец!

Настройка сервера - это не просто. Но ты справишься!

**Совет:** Не бойся экспериментировать. Если что-то сломается - можно всегда переустановить. Данные и другие сервисы на VM не пострадают.

---

Готов начать? Подключайся к серверу (Шаг 1) и погнали! 🚀
