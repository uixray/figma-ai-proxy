# Figma AI Proxy Configuration

# Server port (default: 3001)
PORT=3001

# Node.js environment
NODE_ENV=production

# =====================================================================
# PROXY CONFIGURATION (optional)
# Route outbound requests through a proxy to bypass regional restrictions.
# =====================================================================

# Global proxy for all providers (optional).
# Supported schemes:
#   worker://  — Cloudflare Worker (see cloudflare-worker/README.md)
#   http://    — HTTP/HTTPS proxy (e.g., Squid)
#   socks5://  — SOCKS5 proxy (e.g., SSH tunnel, Dante)
# PROXY_URL=worker://ai-api-proxy.YOUR_NAME.workers.dev

# Per-provider proxy overrides (optional).
# Set to a proxy URL to route only this provider through a proxy.
# Set to "direct" to bypass the global proxy for this provider.
# PROXY_YANDEX=direct
# PROXY_CLAUDE=socks5://127.0.0.1:1080
# PROXY_GEMINI=worker://ai-api-proxy.YOUR_NAME.workers.dev
# PROXY_GROQ=
# PROXY_MISTRAL=
# PROXY_COHERE=

# Auth token for Cloudflare Worker (required if using worker:// proxy).
# Must match the AUTH_TOKEN secret set in the Worker.
# PROXY_AUTH_TOKEN=your-secret-token
